[user]
	name = Bruno Vicenzo
        email = bruno@vicenzo.me
[color]
	ui = auto
[alias]
	br = branch
	cm = commit
	co = checkout
	st = status
	pl = pull
	ps = push
	plr = pull --rebase
	df = diff
	dfc = diff --cached
	dfw = diff --word-diff=plain
	clsorig = remote update origin --prune
        mg = merge
[push]
        default = simple
[core]
	excludesfile = ~/.gitignore_global
[filter "lfs"]
	clean = git-lfs clean %f
	smudge = git-lfs smudge %f
	required = true
[pull]
	ff = only
